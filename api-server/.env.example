# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
DATABASE_URL="file:./dev.db"

# JWT Configuration
# ⚠️ SECURITY WARNING: JWT_SECRET must be a cryptographically strong, unguessable secret.
# - Do NOT use simple strings, "change-in-production" placeholders, or the example value in production.
# - Minimum entropy: ≥256 bits (≥32 bytes) recommended.
# - Generate securely: openssl rand -base64 48 (or equivalent secure random generator).
# - Store securely: environment variables only, secrets manager in production.
# - NEVER commit to source control or expose in logs.
# - Reference the JWT_SECRET and JWT_EXPIRES_IN entries below and replace with actual generated secrets.
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=1h

# Encryption Configuration
# ⚠️ SECURITY WARNING: ENC_KEY is used to encrypt secrets stored in the database.
# - MUST be a 32-byte (256-bit) base64-encoded key for AES-256-GCM encryption.
# - Generate securely: openssl rand -base64 32
# - Store securely: environment variables only, secrets manager in production.
# - NEVER commit to source control or expose in logs.
# - Rotation: Generate new key and re-encrypt all secrets when rotating.
# To generate a new key, run: openssl rand -base64 32
ENC_KEY=your-base64-key-here

# CORS Configuration
# ⚠️ PRODUCTION WARNING: CORS_ORIGIN MUST be set to your actual production domain(s).
# - Do NOT use "localhost" or "*" in production environments.
# - Specify allowlisted origins only, e.g., "https://yourdomain.com".
# - Restrict origins strictly to reduce XSS/CSRF attack surface.
# - Rotate/update values per environment (dev/staging/prod).
CORS_ORIGIN=http://localhost:3000

# Sentry Configuration (Optional)
SENTRY_DSN=
SENTRY_ENVIRONMENT=development

# Docker Configuration
# ⚠️ SECURITY WARNING: Exposing the Docker socket grants root-equivalent access to the host.
# - This can lead to container escape or full host compromise.
# - Use alternatives: Docker contexts, rootless Docker, or dedicated API/proxy.
# - Only mount to trusted processes with restricted permissions.
# - Mount DOCKER_SOCKET_PATH ONLY when absolutely necessary to minimize risk.
DOCKER_SOCKET_PATH=/var/run/docker.sock

# Logging
LOG_LEVEL=info
